[common]
application.directory = APPLICATION_PATH  "/application"
application.dispatcher.catchException = TRUE
application.view.ext = 'tpl'
application.modules = "Index,Cli"
application.library.namespace = "tool"

application.name = 'deal'

application.view.dir = "/application/views"
application.baseDir = APPLICATION_PATH

[DB]
;一主多备
;master
database.master.host = "127.0.0.1"
database.master.user="root"
database.master.password=""
database.master.database="nn"
;slaves
database.slave.0.host = "127.0.0.1"
database.slave.0.user = "root"
database.slave.0.password = ""
database.slave.0.database="nn"


[witty]
witty.tpl_dir = APPLICATION_PATH  "/application/views/"
witty.layout_dir = APPLICATION_PATH  "/application/views/layout/"
witty.compile_dir = APPLICATION_PATH  "/application/runtime/"
witty.cache_dir = APPLICATION_PATH  "/application/cache/"

[routes]
routes.simple.type="simple"
routes.simple.controller=c
routes.simple.module=m
routes.simple.action=a

;正则路由的match要避免写特殊的字符
;routes.reg.type="regex"
;routes.reg.match="#^/list/([a-z]+)-([0-9])[\/]?$#"
;routes.reg.route.module=index
;routes.reg.route.controller=index
;routes.reg.route.action=index
;routes.reg.map.1=name
;routes.reg.map.2=value

;routes.rewrite.type="rewrite"
;routes.rewrite.match="/list/:id/:name"
;routes.rewrite.route.module=cli
;routes.rewrite.route.controller=:id
;routes.rewrite.route.action=index

[develop : common : witty : routes:DB]
[product : common]
